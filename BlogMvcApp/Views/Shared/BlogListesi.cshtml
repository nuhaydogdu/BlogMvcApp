@model  IEnumerable<BlogMvcApp.Models.BlogModel>


@if (Model.Count() == 0)           //eğerki seçilen katagoride blog yoksa alert dönüyoruz
{
    <div class="alert alert-warning">
        <h3>Kayıt Bulunamadı :(</h3>
    </div>
}
else
{
    foreach (var Blog in Model)
    {
        <div class="blog">
            <h3 class="page-header">
                @Blog.Baslik
            </h3>

            <p>
                <span class="glyphicon glyphicon-time"></span>
                @Blog.EklemeTarihi
            </p>

            <hr />
            <img class="img-responsive" src="https://placehold.jp/600x200.png" />
            <hr />

            <p>
                @Blog.Aciklama
            </p>
            @Html.ActionLink("Daha Fazla..", "Details", "Blog", new { id = Blog.Id }, new { @class = "btn btn-primary" })
        </div>
    }
}

